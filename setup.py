from setuptools import setup, find_packages

setup(name='opposite',
      version='1.0',
      description='False',
      author='Mo Mirza',
      url='https://github.com/momirza/opposite',
      packages=find_packages(exclude=['test']),
     )

